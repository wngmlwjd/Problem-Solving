#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int T, N, K;
    vector<int> input;

    cin >> T;

    for(int t = 0;t < T;t++) {
        cin >> N >> K;
        input.assign(N, 0);
        for(int i = 0;i < N;i++) {
            cin >> input[i];
        }

        
    }

    return 0;
}